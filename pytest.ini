[pytest]
testpaths =
    tests
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli=true
markers =
    torch: mark a test as a optional torch test.
log_level=INFO
